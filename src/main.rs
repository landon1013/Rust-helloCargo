fn main() {
    println!("We can build a project using \"cargo build\".");
    println!("We can build and run a project in one step using \"cargo build\".");
    println!("We can build a project without producing a binary to check for errors using \"cargo check\".");
    println!("Instead of saving the result of build in the same directory as our code, Cargo stores it in the target/debug directory.");
}
